## Build Setup

```bash
# install dependencies
npm install

# serve with hot reload at localhost:3000
npm run start

Столкнулся с проблемой загрузки больших фотографий. Решением стало добавление библиотеки react-lazy-load-image-component для ленивой загрузки каждой отдельной фотографии# photo-gallery
